import { DateTime } from 'luxon';

// I put this here so we can easily simulate different times

// export const getCurrentTime = () => DateTime.now();
export const getCurrentTime = () => DateTime.fromISO('2022-06-30');
